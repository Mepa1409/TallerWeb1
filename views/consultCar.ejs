<%- include('./templates/header.ejs') %>
<body>
<%- include('./templates/nav.ejs') %>
<div class="container">
    <br>
    <h2 class="text-center">Buscar Vehiculo</h2>

    <form action="/newConsultCar" method="POST">
        <label for="searchIdCar">Buscar por ID:</label>
        <input type="number" id="searchIdCar" name="searchIdCar" class="form-control"  required>
        <br>
        <button type="submit" class="btn btn-success">Buscar</button>
    </form>
    <div>

        <% if (typeof data!== "undefined" && typeof id!== "undefined")  { %>
            <div class="container p-3">
                <div class="table-responsive">
                    <table class="table table-bordered table-striped mb-3" id="tableData">
                        <thead>
                        <tr class="text-center">
                            <th scope="col">Id</th>
                            <th scope="col">Marca</th>
                            <th scope="col">Valor</th>
                            <th scope="col">Color</th>
                            <th scope="col">Modelo</th>
                            <th scope="col">Eliminar</th>
                        </tr>
                        </thead>
                        <tbody id="tBody">
                        <tr>
                            <form action="/deleteCar" method="POST">
                                <td>
                                    <input type="hidden" name="idCar" value="<%= id %>">
                                    <%= id %>
                                </td>
                                <td><%= data.brand %></td>
                                <td><%= data.value %></td>
                                <td><%= data.color %></td>
                                <td><%= data.model %></td>
                                <td>
                                    <button type="submit" class="btn btn-danger">x</button>
                                </td>
                            </form>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        <% } else { %>
            <br>
            <h4>Vehiculo No Encontrado</h4>
        <% } %>

    </div>


</div>

<%- include('./templates/Footer.ejs') %>
</body>
</html>